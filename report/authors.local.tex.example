% Template: copy this file to authors.local.tex and fill in the real names/IDs/majors.
%   cp authors.local.tex.example authors.local.tex   then edit
% authors.local.tex is excluded by .gitignore and never enters the (public) git repo;
% report.tex auto-loads it (via \IfFileExists) to override the placeholders below.
\renewcommand{\memberA}{Member A}\renewcommand{\sidA}{ID-A}\renewcommand{\majorA}{Major A}
\renewcommand{\memberB}{Member B}\renewcommand{\sidB}{ID-B}\renewcommand{\majorB}{Major B}
